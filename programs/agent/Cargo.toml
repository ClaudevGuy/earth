[package]
name = "earth-agent"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Earth Mandate factory: AI-agent native token with on-chain treasury, operator, and epoch spend cap."

[lib]
crate-type = ["cdylib", "lib"]

[dependencies]
solana-program = "2.1"
