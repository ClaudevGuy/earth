[package]
name = "earth-kernel"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Earth Kernel factory: precompile-style system syscalls (hash, recover, identity) at a reserved slot."

[lib]
crate-type = ["cdylib", "lib"]

[dependencies]
solana-program = "2.1"
