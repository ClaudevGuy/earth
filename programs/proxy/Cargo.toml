[package]
name = "earth-proxy"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Earth Proxy factory: upgradeable token contract. Address stays; implementation rotates after a delay."

[lib]
crate-type = ["cdylib", "lib"]

[dependencies]
solana-program = "2.1"
