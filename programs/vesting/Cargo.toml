[package]
name = "earth-vesting"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Earth vested-lock factory: cliff plus linear unlock, optional clawback."

[lib]
crate-type = ["cdylib", "lib"]

[dependencies]
solana-program = "2.1"
